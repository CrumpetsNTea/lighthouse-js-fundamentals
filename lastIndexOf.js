function lastIndexOf(array, value) {
  var i = array.length;
  for (var i = array.length - 1; i >= 0; i--)
    if (value === array[i]) {
      return i;
    }
}
if (array === undefined || value === undefined){
  var instance = -1
  return instance
}


